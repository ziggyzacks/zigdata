version: 2
jobs:
  build:
    machine:
      docker_layer_caching: true
    steps:
      - checkout
      - run: docker login --username $DOCKER_USERNAME --password $DOCKER_PASSWORD
      - run: docker build -t zigdata .
      - run: docker tag zigdata zigzacks/zigdata:latest
      - run: docker push zigzacks/zigdata
