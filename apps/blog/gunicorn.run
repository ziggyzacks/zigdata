#!/usr/bin/with-contenv sh
cd $HOME/blog/zigdata
lektor build -O /var/www/zigdata
cd $HOME
python -m http.server /var/www/zigdata 8888
# gunicorn wsgi:app --bind 0.0.0.0:8888 --worker-class sanic.worker.GunicornWorker